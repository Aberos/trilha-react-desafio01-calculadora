import styled from 'styled-components';

export const SpanContainer = styled.div`
    position: absolute;
    right: 10px;
    top: -10px;

    span{
        font-size: 10px;
        color: #FFFFFF;
    }
`